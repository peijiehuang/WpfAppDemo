<UserControl x:Class="WpfAppDemo.Views.UserEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" 
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <materialDesign:Card Padding="32" Width="400" HorizontalAlignment="Center" VerticalAlignment="Top" 
                             materialDesign:ElevationAssist.Elevation="Dp4">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,24"/>
                
                <TextBox Margin="0,0,0,16" 
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="{DynamicResource User_HeaderUsername}"
                         Text="{Binding User.Username, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

                <PasswordBox Margin="0,0,0,16" 
                            Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                            materialDesign:HintAssist.Hint="{DynamicResource User_HeaderPassword}"
                            materialDesign:PasswordBoxAssist.Password="{Binding User.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

                <TextBox Margin="0,0,0,16" 
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="{DynamicResource User_HeaderName}"
                         Text="{Binding User.Name, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBox Margin="0,0,0,16" 
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="{DynamicResource User_HeaderEmail}"
                         Text="{Binding User.Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

                <ComboBox materialDesign:HintAssist.Hint="{DynamicResource User_HintRole}" 
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                          Text="{Binding User.Role}"
                          Margin="0,0,0,32">
                    <ComboBoxItem Content="Admin"/>
                    <ComboBoxItem Content="User"/>
                    <ComboBoxItem Content="Guest"/>
                </ComboBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Command="{Binding CancelCommand}" 
                            Style="{StaticResource MaterialDesignOutlinedButton}" 
                            Content="{DynamicResource User_BtnCancel}" Margin="0,0,12,0" Width="100"/>
                    <Button Command="{Binding SaveCommand}" 
                            Style="{StaticResource MaterialDesignRaisedButton}" 
                            Content="{DynamicResource User_BtnSave}" Width="100"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
